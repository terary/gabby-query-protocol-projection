import { ValidateProjectableProperties } from "./ValidateProjectableProperties";
import { ValidateProjectionProperties } from "./ValidateProjectionProperties";

export const Validators = {
  ValidateProjectableProperties,
  ValidateProjectionProperties,
};
